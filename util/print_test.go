package util

import (
	"testing"
)

func TestPrintInfo(t *testing.T) {
	tests := []struct {
		name string
	}{
		{"t"},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			if true {

			}
		})
	}
}
